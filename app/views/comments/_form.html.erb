<%= form_for([@project, Comment.new]) do |f| %>

  <div class='form-group'>
    <%= f.text_area :body, class: 'form-control'%>
  </div>

  <div class="form-group">
      <%= f.submit 'Comment', :class=> 'button-right' %>
  </div>


<% end %>
